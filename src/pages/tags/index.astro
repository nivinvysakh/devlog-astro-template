---
import BaseLayout from '../../layouts/BaseLayout.astro';
import siteConfig from '../../config/site';
import { url } from '../../utils/paths';
import { getCollection } from 'astro:content';
import { Tag } from 'lucide-astro';

const allPosts = await getCollection('blog');
const tags = [...new Set(allPosts.flatMap((p) => p.data.tags || []))];

const tagsWithCount = tags.map((tag) => ({
  name: tag,
  count: allPosts.filter((p) => p.data.tags?.includes(tag)).length,
})).sort((a, b) => b.count - a.count);
---

<BaseLayout
  title={`Tags Directory — ${siteConfig.name}`}
  description="Search and explore technical notes and publications by technology tags."
>
  <div class="mb-10 p-7 sm:p-10 liquid-glass rounded-3xl shadow-xl">
    <div class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold bg-indigo-500/10 text-indigo-700 dark:text-indigo-300 border border-indigo-500/20 mb-2.5">
      <Tag class="w-3.5 h-3.5" />
      Topic Tags
    </div>
    <h1 class="text-3xl sm:text-4xl font-extrabold text-zinc-900 dark:text-zinc-50 tracking-tight">
      Explore by Tag
    </h1>
    <p class="text-sm text-zinc-600 dark:text-zinc-400 mt-2">
      Filter through publications by topic, tool, framework, and technique.
    </p>
  </div>

  <div class="p-7 sm:p-10 liquid-glass rounded-3xl shadow-xl">
    <div class="flex flex-wrap gap-3">
      {tagsWithCount.map((tag) => (
        <a
          href={url(`/tags/${encodeURIComponent(tag.name.toLowerCase())}`)}
          class="flex items-center gap-2 px-4 py-2 rounded-full liquid-pill hover:scale-105 hover:border-indigo-400/40 transition-all text-xs sm:text-sm font-medium"
        >
          <span class="text-indigo-500 dark:text-indigo-400 font-mono text-xs font-semibold">#</span>
          <span class="text-zinc-800 dark:text-zinc-200">{tag.name}</span>
          <span class="ml-1 px-2 py-0.5 text-[11px] bg-white/40 dark:bg-white/10 text-zinc-700 dark:text-zinc-300 font-mono rounded-full">
            {tag.count}
          </span>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>
